import {
    Box
} from "@mui/material";
import incentive_status from "./incentive_status";

export default function incentive() {
    return (
        <Box>
            <incentive_status />
        </Box>
    );
}