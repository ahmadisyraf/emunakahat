import {
    Box
} from "@mui/material";
import IncentiveStatus from "./incentive_status";

export default function Incentive() {
    return (
        <Box>
            <IncentiveStatus />
        </Box>
    );
}